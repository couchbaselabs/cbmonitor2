apiVersion: 1

# The single `prometheus` datasource is managed by the cbmonitor app plugin
# itself (reconciled at runtime from app jsonData via the Grafana HTTP API).
# It points at the datasource-gateway sidecar when the gateway is enabled,
# otherwise at Prometheus/Mimir directly. Don't add it back here or you'll get
# duplicates.
datasources: []
