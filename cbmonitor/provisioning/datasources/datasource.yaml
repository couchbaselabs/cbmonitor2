apiVersion: 1

datasources:
- name: Capella Db1
  type: couchbase-datasource
  access: proxy
  jsonData:
    host: ${COUCHBASE_HOST}
    username: ${COUCHBASE_USERNAME}
  secureJsonData:
    password: ${COUCHBASE_PASSWORD}
