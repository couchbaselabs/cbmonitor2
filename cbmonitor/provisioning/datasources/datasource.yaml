apiVersion: 1

datasources:
- name: cbdatasource
  type: couchbase-datasource
  access: proxy
  jsonData:
    host: ${COUCHBASE_HOST}
    username: ${COUCHBASE_USERNAME}
  secureJsonData:
    password: ${COUCHBASE_PASSWORD}
