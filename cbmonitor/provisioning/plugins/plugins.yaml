apiVersion: 1

# No plugins to enable here. The cbmonitor app plugin itself is provisioned
# via plugins/apps.yaml; datasource plugins (prometheus, couchbase-datasource)
# come from Grafana's built-ins / bundled includes.
plugins: []
